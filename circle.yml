machine:
  environment:
    HOGE: "aa" #for avoiding circle CI format error

dependencies:
  override:
    - sudo chmod 777 /opt/
    - yes | gcloud components install app-engine-go
    - ./commands/setup.sh
    - ./commands/glide_update.sh

test:
  override:
    - ./commands/sample1_test.sh
